# Sudoku — Project scaffold

This workspace contains an initial scaffold for the Sudoku project. Backend starter is in `backend/`.

Quick commands (backend):

Install dev deps and run tests:

```bash
cd backend
npm install
npm test
```

Run quick demonstration (requires `ts-node`):

```bash
cd backend
npx ts-node src/index.ts
```

Next steps:
- Implement generator and pre-generation script
- Add API server and persistence
- Scaffold frontend (React + Vite)
